<p id="notice"><%= notice %></p>

<h1>Поезд №<%= @train.number %></h1>

<strong>Вагоны:</strong>
<p>
  <% if @train.wagons.any? %>
    <!--<ul>-->
      <%# @train.wagons.ordered(@train.sort_from_head).each do |wagon| %>
          <!--<li><%#= link_to "Вагон №#{wagon.number}", train_wagon_path(@train, wagon) %></li>-->
          <!--<li><%#= link_to "Вагон №#{wagon.number}", wagon_path(wagon) %></li>-->
      <%# end %>
      <table>
        <tr>
          <th>Номер вагона</th>
          <th>Тип вагона</th>
          <th>Поезд</th>
          <th colspan="3"></th>
        </tr>

      <%= render partial: 'wagons/wagon', collection: @train.wagons.ordered(@train.sort_from_head) %>
      </table>
    <!--</ul>-->
<% else %>
    нет
<% end %>
</p>

<p>
  <%= link_to 'Добавить вагон', new_train_wagon_path(@train) %>
</p>

<p>
<strong>На станции:</strong>
<% if @train.current_station %>
      <%=	@train.current_station.title %>
<% else %>
      Не указано
<% end %>
</p>

<p>
<% if @train.route %>
      <strong>Маршрут:</strong>
      <%= @train.route.title %>
<% else %>
      Маршрут не назначен
<% end %>
</p>

<%= link_to 'Редактировать', edit_train_path(@train) %> |
<%= link_to 'Назад', trains_path %>
